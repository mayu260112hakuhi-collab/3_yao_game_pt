---
title: "【八百万駆動】日本語スクリプト（.8g）構文およびエンジン処理仕様"
emoji: "📜"
type: "tech"
topics: ["rust", "tauri", "dsl", "script"]
published: false
---

# 【八百万駆動】日本語スクリプト（.8g）構文およびエンジン処理仕様

## 1. 専用拡張子・役割
- `.8g`: 日本語記述スクリプトファイル（ゲームロジック・イベント記述）。
- `.8gsn` / `.8wsn`: シーン定義ファイル（マスターシーン／サブシーン構成）。
- `.8game`: パッケージ化されたゲームプロジェクトファイル。

## 2. 構文と命令種別（命令種別 Enum）
Rustバックエンド（`yaoyorozu_core`）にて解析・実行される命令の一覧および構文例。

### 主な命令種別
- **表示**: `（メッセージ）を表示。`
- **移動 / 座標**: オブジェクトの位置操作および座標取得。
- **取得**: `（パラメータ）を取得。`
- **ディレクトリ内の画像を表示**: 指定フォルダ内の画像を自動検出してUI描画。
- **保存 / 作成 / 削除 / 開く / 閉じる / 設定 / 待機 / ドラッグ / ドロップ / 拡大 / 縮小 / 回転**: 各種エディタ・ゲーム内操作。

## 3. パーサー・実行ロジックの構造
- **`command.rs`**: 命令のデータ構造（`命令` 構造体、`命令種別` Enum）の定義。
- **`parser_jp.rs`**: 日本語ソースコード文字列を正規表現（Regex）や構文判定で解析し、命令構造体に変換。
- **`eval.rs` / `executor.rs`**: 解析された命令を受け取り、動詞に基づく `match` 処理で実行。
- **`fs_ops.rs`**: ファイル読み込み・パス解決・重複インクルード防止（`include_once`）の制御。