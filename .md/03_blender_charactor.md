「ボクセルマップ × アニメ風キャラ」の組み合わせでキャラメイクを実現する場合、「ボクセルのマス目基準で作るモジュール分割」が鍵になります！

アニメ風の丸み・シルエットを活かしつつ、ボクセルの世界観とすり合わせるための素体制作の手順をまとめました。

1. 素体のモジュール分割（パーツ構造）
アニメ風キャラメイクでは、以下の 5つの部位 にオブジェクトを分割して作成します。

Plaintext
[髪型パーツ]  ─── 髪型A / 髪型B (差し替え用)
    │
[頭部素体]   ─── 顔・目（表情アニメーションや目の色替え）
    │
[胴体素体]   ─── インナー（服を上から被せる、または服パーツと差し替え）
    │
[腕・手]     ─── グローブや袖の差し替え単位
    │
[脚・足]     ─── 靴やボトムスの差し替え単位
2. Blenderでの具体的な作成手順
Step 1: ボクセルの「グリッド感」にサイズを合わせる
ボクセルの基本単位（例: 1ブロック = 1m）を決めておき、キャラの身長が2〜2.5ブロック分におさまるようデフォルメ（2.5〜3.5頭身）のシルエットで作ります。

Step 2: 共通のアーマチュア（骨組み）を1つ配置
Shift + A ➔ 「アーマチュア」 を追加し、人型の骨組みを作成します。

今後作成するすべての着せ替えパーツ（髪、服、装備）は、この1つの骨組みに対してウェイト（スキンウェイト）を付与します。

Step 3: 重なり防止（貫通対策）のコツ
服を着せた時に中の素体ポリゴンがはみ出さないよう、「服で隠れる胴体部分」は描画用のメッシュを削るか、Bevy側で非表示にできる設計にしておきます。

3. Bevy（Rust）へ持って行くときのアプローチ
作成した素体とパーツ群をプログラム側で扱う方法は2通りあります。

パーツ別々ファイル（推奨）:

body_base.glb, hair_01.glb, hair_02.glb のように個別にエクスポート。

プレイヤーがキャラメイク画面で選んだパーツの .glb だけを Bevy 側で動的に読み込んで、同じ骨（Entity）にアタッチする。

1つの .glb にまとめて非表示切り替え:

全パーツを入れた character.glb を書き出し、選択されていないパーツの Visibility を Bevy 側で Hidden に切り替える。

まずはBlenderで 「簡単なデフォルメ頭身ののっぺらぼう素体」 を1体作り、そこに重ねる 「髪型パーツ（1つ）」 を別オブジェクトとして作ってみることから始めてみてください！
